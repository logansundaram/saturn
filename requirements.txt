# Saturday.ai dependencies.
# Pinned to the versions developed against; relax the bounds if you need to.
# Install with:  pip install -r requirements.txt

# --- core agent framework ---
langgraph>=1.2.0
langchain>=1.3.1
langchain-core>=1.4.0
langchain-ollama>=1.1.0
langchain-text-splitters>=1.0.0   # RAG chunking (stores/rag.py) - not pulled in transitively
langgraph-checkpoint-sqlite>=3.1.0
ollama>=0.6.2                 # local-model discovery (/models)

# --- knowledge base / documents ---
pypdf>=6.12.2                 # PDF ingestion for the RAG corpus

# --- web tools (keyless by default) ---
ddgs>=9.14.4                  # keyless DuckDuckGo search
trafilatura>=2.0.0            # local page-content extraction
httpx>=0.28.1
tavily-python>=0.7.24         # OPTIONAL: premium web search/extract/research

# --- cloud providers (OPTIONAL: only if a tier binds a role to a cloud provider) ---
# langchain-anthropic>=1.0.0    # provider: anthropic (the cloud-hybrid tier)
# langchain-openai>=1.0.0       # provider: openai

# --- config / utilities ---
pyyaml>=6.0.3
python-dotenv>=1.2.2

# --- terminal UI ---
rich>=15.0.0
prompt_toolkit>=3.0.52        # OPTIONAL: live /command highlighting at the prompt
psutil>=7.2.2                 # CPU/RAM/GPU metrics in the status bar
