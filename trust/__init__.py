"""The trust stack — the product. One module per concern: the gate policy object (policy), the
network chokepoint + egress ledger + inference-locality classifier (egress), cloud-boundary
secret stripping (redaction), prompt-injection quarantine (quarantine), the per-answer trust
receipt + session posture line (receipt), and answer-level provenance — the Glass Box (glassbox).
"""
